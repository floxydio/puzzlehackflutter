import 'package:flutter/material.dart';

class SecondPuzzle extends StatefulWidget {
  const SecondPuzzle({ Key? key }) : super(key: key);

  @override
  _SecondPuzzleState createState() => _SecondPuzzleState();
}

class _SecondPuzzleState extends State<SecondPuzzle> {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text("Cokro 2"))
    );
  }
}